// Code generated by protoc-gen-go-orm. DO NOT EDIT.
// versions:
//  protoc-gen-go-orm {{.Version}}
//  protoc           {{.ProtocVersion}}
// source: {{.Source}}
syntax = "proto3";
package {{.DBType}};

option go_package = "{{.GoImportPath}}/internal/{{.DBType}}";

{{- range .Messages}}
{{if .OrmOptions.IsTable}}{{template "kv_message" .}}{{end}}
{{ end -}}

{{- define "kv_message" -}}
// {{.Name}} {{.Comment}}
// KV 存储记录（PAYLOAD：键字段 + data）
message {{.Name}} {
  {{- $msg := . -}}
  {{ range .Fields -}}
  {{- $f := . -}}
  {{if or ($f.OrmOptions.HasPrimaryKey) ($f.OrmOptions.HasIndex) ($f.OrmOptions.HasVersion) -}}
  {{if $f.Comment -}}// {{$f.Comment}}{{- end }}
  {{getDBFieldType $f}} {{$f.Name}} = {{$f.Number}};
  {{- end }}
  {{- end }}
  bytes data = 999;
}
{{- end -}}
